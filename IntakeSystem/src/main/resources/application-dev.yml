server:
  domain: localhost
  port: 8081

spring:
  servlet:
    multipart:
      location: E:\multipart

avatar:
  path:
    upload: "E:/avatar"
    access: "/avatar/"

logging:
  level:
    com:
      student_online:
        IntakeSystem:
          mapper: DEBUG